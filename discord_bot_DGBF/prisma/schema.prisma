
datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

generator client {
  provider = "prisma-client-js"
}

model User {
  id        String  @id
  xp        Int     @default(0)
  zeni      Int     @default(1000)
  warns     Int     @default(0)
}
